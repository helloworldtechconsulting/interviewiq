// App.tsx is intentionally minimal.
// All routing and providers are set up in main.tsx.
// This file exists only so Vite's default template reference doesn't break
// during migration — it is not rendered anywhere.
export default function App() {
  return null;
}
